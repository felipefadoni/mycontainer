import { styled } from "src/styles";

export const PageTitleContainer = styled("div", {
  display: "block",
  marginBottom: 16,

  h1: {
    fontSize: "2rem",
    fontWeight: 700,
    color: "$pinkDark",
  },
});
